# Python
__pycache__/
*.py[cod]
*$py.class

# Virtual environment
.venv/
env/

# Django
backend/db.sqlite3
*.sqlite3
backend/media/
backend/staticfiles/
local_settings.py

# Environment files
.env
.env.*
!.env.local.example
!frontend/.env.local.example
!backend/.env.example

# Face recognition training data (raw images — not needed at runtime, embeddings
# already encode them). The trained .pkl artifacts below ARE tracked: they cannot be
# regenerated on the deploy host (no training data, no GPU, offline) and the runtime
# loads them directly (face_recognition/check.py, accounts/views.py predict).
backend/services/face_recognition/training_data/

# Next.js
frontend/.next/
frontend/node_modules/
frontend/.env.local

# Logs
*.log
logs/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
.pytest_cache/
.mypy_cache/
.coverage
*.swp
*.swo
.ropeproject/
.pylint.d/
.python-version
